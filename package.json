{
  "name": "cpp-compiler",
  "version": "1.0.0",
  "description": "C++ compiler",
  "main": "server.js",
  "scripts": {
    "test": "node server.js",
    "start": "cd client && npm install && npm run build && cd .. && node server.js",
    "runDev": "nodemon server.js",
    "uibuild": "cd client && npm install && npm run build && cd ..",
    "push" : "git add . && git commit -m 'UI Build' && git push"
  },
  "author": "manishsencha",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
